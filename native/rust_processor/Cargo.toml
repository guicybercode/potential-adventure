[package]
name = "rust_processor"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_processor"
crate-type = ["cdylib", "rlib"]

[dependencies]
rustler = "0.32"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
